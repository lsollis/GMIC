api_version: 3
name: gmic-localhost-fedavg
description: |
  This is a configuration file for the GMIC Federated Learning framework.
  It defines the parameters for the federated learning process, including
  the number of clients, server settings, and training parameters.

participants:
  - name: overseer.example.com
    type: overseer
    org: UHCC
    protocol: https
    host: localhost
    api_root: /api/v1
    port: 8443
  - name: localhost
    type: server
    org: UHCC
    host: localhost
    fed_learn_port: 8102
    admin_port: 8103
  - name: UHCC
    type: client
    org: UHCC
  - name: Moffitt
    type: client
    org: Moffitt
  - name: lsollis@cc.hawaii.edu
    type: admin
    org: UHCC
    role: project_admin

# The same methods in all builders are called in their order defined in builders section
builders:
  - path: nvflare.lighter.impl.workspace.WorkspaceBuilder
    args:
      template_file: master_template.yml
  # - path: nvflare.lighter.impl.template.TemplateBuilder
  - path: nvflare.lighter.impl.static_file.StaticFileBuilder
    args:
      config_folder: config
      docker_image: gmic-gmic_fl
      overseer_agent:
        path: nvflare.ha.dummy_overseer_agent.DummyOverseerAgent
        # if overseer_exists is true, args here are ignored.  Provisioning
        #   tool will fill role, name and other local parameters automatically.
        # if overseer_exists is false, args in this section will be used.
        overseer_exists: false
        args:
           sp_end_point: localhost:8102:8103
           heartbeat_interval: 6
  - path: nvflare.lighter.impl.cert.CertBuilder
  - path: nvflare.lighter.impl.signature.SignatureBuilder